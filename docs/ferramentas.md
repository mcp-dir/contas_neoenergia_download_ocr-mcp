# Ferramentas

Neoenergia (Elektro): Download + OCR expõe 1 ferramenta (todas somente leitura).

### 1. `contas_neoenergia_download_ocr_consultar`
**Input**: `login_cpf` (opcional), `login_cnpj` (opcional), `login_senha`, `uf` (opcional), `uc` (opcional), `mes_ano` (opcional), `acesso_imobiliaria` (opcional)

Neoenergia (Elektro): Download + OCR, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar Neoenergia (Elektro): Download + OCR
```
